server:
  port: 8080

spring:
  application:
    name: personal-website
  
  datasource:
    url: jdbc:mysql://192.168.3.11:3306/personal_website?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: cdh.2580
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: your-256-bit-secret-key-for-jwt-token-generation-min-32-characters
  expiration: 86400000

cors:
  allowed-origins: http://localhost:3000,http://localhost:5173

logging:
  level:
    com.personal.website: DEBUG
